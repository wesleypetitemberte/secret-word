export default function StartScreen() {
    return (
        <section className="start">
            <h1>Secret Word</h1>
            <p>Clique no botão abaixo para jogar!</p>
            <button>JOGAR</button>
        </section>
    );
}
